<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="egovframework.example.sample.library.service.impl.LoginMapper">

	<resultMap id="loginVO"
		type="egovframework.example.sample.library.service.LoginVO">
		<id property="memberId" column="member_id" />
		<result property="username" column="username" />
		<result property="password" column="password" />
		<result property="name" column="name" />
		<result property="role" column="role" />
	</resultMap>
	
	
	<select id="selectLoginUser" parameterType="loginVO"
		resultType="loginVO">
		SELECT
		member_id as memberId,
		username as username,
		password
		as password,
		name as name,
		role as role
		FROM member
		WHERE username =
		#{username} AND password = #{password}
	</select>
	
	
	<select id="selectUserByUsername" parameterType="String"
		resultMap="loginVO">
		SELECT
		member_id, username, password, name, role
		FROM
		member
		WHERE
		username = #{username}
	</select>
</mapper>